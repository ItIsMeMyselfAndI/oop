from frontend.styles.base import scale


# ---- exclusive for save button ----
class SaveStyles:
    SAVE_POPUP_WIN_W = scale(400)
    SAVE_POPUP_WIN_H = scale(200)

    SAVE_BTN_W = scale(350)
    SAVE_BTN_H = scale(60)